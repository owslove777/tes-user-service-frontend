import React from 'react';

const TalentProduction = (props) => {

  return (
  //   <select>
  //   <option key={props.id} value={props.id}>{props.id}</option>
  // </select>

<ui>
    <h2> {props.id} / {props.talentId} / {props.dateTime} / {props.price} / {props.status}</h2>
</ui>

  )

};
export default TalentProduction;
